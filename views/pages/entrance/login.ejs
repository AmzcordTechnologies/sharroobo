

<div class="row">

  <div class="col-2">

  </div>
  <div class="col-8">
    <div id="login" v-cloak >
      <div class="col-sm-12 col-md-12">
        <h3 class="text-center title"><b>👋 Ravis de vous revoir ! Connectez-vous à votre espace</b></h3>
        <div style="max-width: 450px;" class="mx-auto">
          <!-- <hr/> -->
          <div class="formbody">
            <ajax-form action="login" :syncing.sync="syncing" :cloud-error.sync="cloudError" :form-data="formData" :form-rules="formRules" :form-errors.sync="formErrors" @submitted="submittedForm()">
              <div class="form-group">
                <input type="email" class="form-control" placeholder="Votre email" :class="[formErrors.emailAddress ? 'is-invalid' : '']" v-model.trim="formData.emailAddress" autocomplete="email" focus-first>
                <div class="invalid-feedback" v-if="formErrors.emailAddress">Please provide a valid email address.</div>
              </div><br>
              <div class="form-group">
                <input type="password" class="form-control" placeholder="Votre mot de passe" :class="[formErrors.password ? 'is-invalid' : '']" v-model.trim="formData.password" autocomplete="current-password">
                <div class="invalid-feedback" v-if="formErrors.password">Please enter your password.</div>
              </div>
              <div class="form-group form-check text-right">
                <!-- <input class="form-check-input" type="checkbox" id="remember" name="rememberMe" v-model="formData.rememberMe"/> -->
                <a href="#"><label class="form-check-label" for="remember" style="color:#4d50f4"><b>Mot de passe oublié ?</b></label></a>
              </div>
              <cloud-error v-if="cloudError==='badCombo'">The credentials you entered are not associated with an account. Please check your email and/or password and try again.</cloud-error>
              <cloud-error v-else-if="cloudError"></cloud-error>
              <div class="row submitbutton">
                <div class="col-6">
                  <ajax-button :syncing="syncing" class="btn-dark btn-lg btn-block"><b>Suivant</b></ajax-button>
                </div>
                <div class="col-6 Forgot text-black">
                  <a href="/password/forgot"><i class="fa fa-chevron-left" aria-hidden="true"></i> &nbsp;&nbsp;ou Entrée</a>
                </div>
              </div>
              <div class="form-group">
              </div>
            </ajax-form>
          </div>
        </div>
      </div>
    </div>
  </div>
  <div class="col-2">

  </div>

</div>



<%- /* Expose locals as `window.SAILS_LOCALS` :: */ exposeLocalsToBrowser() %>
